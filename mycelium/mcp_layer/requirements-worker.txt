# Minimal dependencies for the immune daemon and SSS relay
# urllib is stdlib — no external HTTP library needed
# oqs-python is installed from source in the Containerfile
cffi>=1.12
cryptography==41.0.3
psycopg2-binary
requests
